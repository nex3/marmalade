(1
  <% _(packages).each(function(pkg) { %>
    (
      <%- symbol(pkg.name) %> .
        [
          <%- list(pkg.version) %>
          <%- list(pkg.requires) %>
          <%- string(pkg.description) %>
          <%- pkg.type %>
        ]
     )
  <% }); %>
)
